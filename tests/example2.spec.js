describe('SPEC2', function () {

  it('SPEC2 - TC1', function() {
    console.log(`OS : ${process.env.OS} OS VERSION : ${process.env.OS_VERSION} BROWSER NAME : ${process.env.BROWSERNAME} BROWSER VERSION : ${process.env.BROWSER_VERSION}`);
  });

});
